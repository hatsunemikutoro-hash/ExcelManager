📊 Excel Manager CLI
Excel Manager é uma ferramenta de linha de comando (CLI) desenvolvida em Python para automatizar a criação e o preenchimento de planilhas .xlsx. O foco é produtividade: eliminar o trabalho manual repetitivo de abrir o Excel para tarefas simples de entrada de dados.

🚀 Funcionalidades (MVP)
Criação Inteligente: Gera planilhas do zero a partir de uma string de dados.

Distribuição Automática: Organiza os dados em colunas baseado na entrada do usuário (ex: define cabeçalhos e preenche as linhas correspondentes).

Preenchimento Rápido: Adiciona novos dados a planilhas já existentes de forma incremental.

Tratamento de Erros: Valida entradas numéricas para evitar crashes durante a execução.

🛠️ Tecnologias Utilizadas
Python 3.x

Bibliotecas de Manipulação: Openpyxl (ou a que você estiver usando no core.py)

Estrutura: Modular (Separação entre lógica de interface e lógica de negócio)

📖 Como Funciona?
1. Criar Planilha
Ao informar os dados e o número de colunas, o programa faz o mapeamento automático.

Entrada: nome, idade, joao, maria, 32, 32 | Colunas: 2

Resultado:
| nome | idade |
| :--- | :--- |
| joao | 32 |
| maria | 32 |

2. Preencher Planilha
Ideal para quem já possui o template pronto e quer apenas inserir novas linhas sem abrir o editor de planilhas.

🛠️ Próximos Passos (Backlog)
[ ] Implementar busca de registros por termo.

[ ] Adicionar contador de linhas e visualização de cabeçalho.

[ ] Proteção contra CSV Injection (Segurança de Dados).

Por que isso faz diferença?
Markdown: Usar tabelas e listas deixa o projeto com cara de "profissional".

Backlog: Mostrar o que você pretende fazer (aquelas ideias que salvamos na lista!) prova que você tem visão de produto e não apenas "copiou um código".

Terminologia: Usar termos como "Incremental", "Modular" e "Validação" mostra que você conhece o vocabulário da área.

Quando você acordar e for mexer no código, quer que eu te ajude a escrever a lógica da função buscarRegistro? Ela é a que mais impressiona em entrevistas por envolver filtros de dados.
